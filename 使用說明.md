# 成語填空遊戲 - 開發與使用說明

## 專案簡介
這是一個現代化的「成語填空遊戲」，專為國小學生設計。
專案已從單一 HTML 升級為 **Vite 模組化架構**，具備高效的建置流程、自動化部署 (CI/CD) 以及豐富的 UI/UX 回饋系統。

## 檔案結構
- `index.html`：專案入口，包含 Service Worker 清除機制與 Favicon 連結。
- `src/main.js`：核心遊戲邏輯，整合了 `canvas-confetti` 特效與 Web Audio API 音效。
- `src/style.css`：基於 Tailwind CSS v4 的樣式定義，包含毛玻璃特效與自訂動畫。
- `src/data/idioms.js`：獨立題庫檔案，方便未來擴充。
- `public/`：存放靜態資源，如 `favicon.svg`。
- `.github/workflows/deploy.yml`：GitHub Actions 自動化部署設定。

## 核心功能與特色
1. **工程化架構**：使用 Vite 作為開發伺服器與打包工具，支援熱更新 (HMR)。
2. **自動化部署**：推送到 GitHub 後，自動打包並發布至 GitHub Pages。
3. **沉浸式體驗**：
   - **視覺**：答對時觸發撒花特效；計時條具備顏色顏色警示與平滑動畫。
   - **聽覺**：完善的答對、答錯、倒數計時音效回饋。
4. **安全防護**：導入隱私金鑰佔位符機制，確保開發過程中無敏感資料外洩。

## 如何執行與開發
1. **本機啟動**：
   ```bash
   npm install  # 安裝依賴 (如 canvas-confetti)
   npm run dev  # 啟動開發伺服器
   ```
2. **正式打包**：
   ```bash
   npm run build # 產出 dist 資料夾
   ```

## 遊戲玩法流程
1. **進入遊戲**：首頁自動加載第一題。
2. **觀察題目**：畫面顯示缺字的成語與解釋。
3. **提交答案**：在輸入框填寫正確字後，點擊「確定」或按 `Enter`。
4. **互動反饋**：觀察撒花效果或聽取錯誤提示。
5. **進度追蹤**：遊戲會紀錄本次遊玩的得分，挑戰最高紀錄！

---
> [!NOTE]
> **線上遊玩網址**：`https://cagoooo.github.io/typeTC/`
