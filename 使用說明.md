# 成語填空遊戲 - 開發與使用說明

## 專案簡介
這是一個僅由單一 HTML 檔案構成的「成語填空遊戲」。
使用純前端技術（HTML, CSS, JavaScript）並透過 CDN 引入 Tailwind CSS 快速打造響應式 UI，加上 Google Fonts (Noto Sans TC) 的字體搭配。

## 檔案結構
此專案結構非常簡單：
- `typeTC.html`：包含 HTML 結構、CSS 樣式（包含 Tailwind JIT 編譯後的樣式）以及負責遊戲邏輯的 JavaScript 代碼。全檔案大約 360 行。

## 核心功能與特色
1. **隨機題庫**：內建 50 個國小程度的成語題庫字串陣列（包含成語與解釋），遊戲每次隨機挑選題目。
2. **限時挑戰機制**：每題預設有 15 秒的時間限制，並配有動態倒數進度條。當時間剩下 5 秒以下時，進度條會變成橘色警告；超時則變紅色並自動結算錯誤。
3. **即時互動反饋**：
   - 畫面中央顯示一個四字成語，其中一個字呈現空白並帶有 CSS `pulse` 閃爍特效。
   - 在底部的輸入框填入缺漏字，答對加 10 分並顯示綠色邊框特效與提示語；答錯或超時則公佈正確答案與紅色邊框及錯誤提示。
4. **流暢體驗**：不僅支援點擊按鈕，也支援在輸入框按下鍵盤 `Enter` 快速提交答案。
5. **RWD 設計**：版面使用 Tailwind CSS 的 breakpoint (`sm:`, `md:`) 設定，支援手機、平板或各式裝置尺寸。

## 如何執行與測試
由於此專案是純靜態的 HTML 檔案，不依賴任何 Node.js 環境、後端伺服器或建置工具（Build tools）。

**最簡單的執行方式：**
1. 無需安裝任何套件。
2. 直接點滑鼠兩下開啟 `typeTC.html`，或是將檔案拖曳到任何現代瀏覽器（Chrome, Edge, Safari, Firefox）中即可開始遊戲。

## 遊戲玩法流程
1. 開啟頁面後，遊戲會自動初始第一題，計時器開始倒數。
2. 畫面會顯示四字成語，但缺漏其中一個字。
3. 在下方的輸入框中填寫你認為正確的字。
4. 點擊「確定」按鈕或按下鍵盤的 `Enter` 鍵提交。
5. 觀看答題結果（加分或顯示正解）。
6. 點擊「下一題」繼續挑戰，或隨時點擊「重新開始」將分數歸零並重新遊玩。
