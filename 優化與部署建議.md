# 成語填空遊戲：開發進度與未來優化建議

## 🟢 目前開發進度表 (Progress)

| 階段 | 任務目標 | 狀態 | 說明 |
| :--- | :--- | :---: | :--- |
| **階段 0** | **核心單檔開發** | ✅ 完成 | 完成純 HTML/CSS/JS 的單檔成語遊戲，具備基礎計時、作答、判定功能。 |
| **階段 1** | **專案工程化 (Vite + 模組化)** | ✅ 完成 | 已導入 Vite 建置工具，拆分 JS / CSS / HTML，並將題庫獨立為 `src/data/idioms.js`，加入 Tailwind CSS 配置。 |
| **階段 2** | **自動化部署與 GitHub Pages 上線** | ⏳ 待開發 | 建立 CI/CD 工作流，打包目前已完成的 Vite 架構並自動發布到 GitHub Pages。 |
| **階段 3** | **遊戲內容與體驗優化 (UI/UX)** | ⏳ 待開發 | 增加答對/答錯音效、微動畫、成語釋義動態展示、難度分級等。 |
| **階段 4** | **進階功能與 Firebase 後端整合** | ⏳ 待開發 | 整合 Firebase 以實作全域排行榜、會員登入、雲端題庫管理。 |
| **階段 5** | **PWA 支援與教育延伸功能** | ⏳ 待開發 | 導入 Service Worker 離線遊玩、錯題本、學習歷程分析、社群圖卡分享等。 |

---

## 🚀 後續優化與改良詳細建議

基於目前**已完成的 Vite 模組化架構（階段 1）**，系統的底層建設已經非常穩固。後續的開發與優化可以往以下幾個深度方向進行：

### 階段 2：自動化部署與 GitHub Pages 上線 (最優先建議)
目前本機開發環境已經就緒，建議先打通部署流程，讓遊戲隨時可以線上預覽。
* **行動方案**：
  1. **建立 GitHub Repo 並推送代碼**：將本地端資料夾初始化為 Git 儲存庫並推送到 GitHub。
  2. **設計 CI/CD 自動化部署**：建立 `.github/workflows/deploy.yml`。設定當推送到 main 分支時，GitHub Actions 會自動執行 `npm run build`，並將編譯後的 `/dist` 產物自動推送到 `gh-pages` 分支。
  3. **基礎安全防護準備**：在開始撰寫後端與串接外部 API 之前，確認 `.gitignore` 設置正確，避免未來不小心將環境變數外洩。

### 階段 3：遊戲內容與體驗優化 (UI/UX) - 強化遊玩回饋
目前畫面已經很漂亮，但缺少聽覺與豐富的「動態」回饋感。
* **行動方案**：
  1. **成語釋義動態展示**：
     - 在解答區下方新增一個區塊，無論答對或答錯，皆以動畫（如 Fade-in）顯示該成語的**解釋 (`meaning` 欄位)**，讓遊戲兼具教育意義。
  2. **微動畫與音效增強**：
     - **聽覺**：加入簡單的 Web Audio API 提示音（答對時的「叮咚」聲、答錯的「叭叭」聲、倒數 5 秒的急促滴答聲）。
     - **視覺**：答對時加入粒子特效（例如 Confetti 撒花效果）；答錯或超時加入畫面震動（Shake）動畫。
  3. **難度與模式擴充**：
     - **無盡生存模式**：初始給予 3 顆愛心，錯一次扣 1 顆，挑戰最高連對次數。
     - **計分倍率系統**：連續答對（Combo）時獲得額外加分，答題速度越快分數越高。

### 階段 4：整合 Firebase 後端服務 - 打造 Serverless 應用
這部分非常契合您的開發規範，將純前端遊戲升級為具有資料持久性的網頁應用。
* **行動方案**：
  1. **全域記分板 (Firestore)**：
     - 遊戲結算時跳出模態框（Modal）讓玩家輸入暱稱。
     - 使用 Firebase Firestore 寫入分數與遊玩時間，並在首頁建立一個即時更新的「Top 10 榮譽排行榜」。
  2. **老師/管理員後台 (Firestore + Auth)**：
     - 結合 Firebase 登入機制，區分「一般玩家」與「老師」。
     - 老師可以登入簡易後台，直接在網頁介面上新增、修改題庫（寫入 Firestore），前端再即時從雲端拉取題庫，達成**不必修改代碼也能更新題目**的目標。
  3. **玩家學習歷程紀錄**：
     - 若學生使用 Google 登入，可記錄他們常錯的成語，期末老師可匯出「常錯成語分析報表」。
  4. **全域金鑰防護 (嚴格執行)**：
     - 落實佔位符腳本 `<__PLACEHOLDER__>` 機制，將 Firebase API Key 等機密資訊防禦在 GitHub Repository Secrets 內，並透過 Actions 注入，確保源碼 100% 安全。

### 階段 5：現代化 PWA 支援與教育社交延伸 (長遠規劃)
* **行動方案**：
  1. **PWA (漸進式網頁應用) 支援**：
     - 透過 Vite 的 PWA 外掛 (`vite-plugin-pwa`)，自動生成 `manifest.json` 與 Service Worker。
     - 學生可以用手機或平版的瀏覽器直接將遊戲「加入主畫面」，獲得全螢幕、無網址列、甚至支援離線遊玩的 App 等級原生體驗。
  2. **「我的錯題本」功能**：
     - 將玩錯的成語自動收集到「錯題本」專區，並提供專門針對錯題的複習模式測驗。
  3. **成績圖卡生成與社群分享**：
     - 遊戲結算時產生一張「成績圖卡」（可使用 `html2canvas` 庫生成），讓玩家可以一鍵分享到 LINE 或 Facebook 邀請同樂。

---

## 💡 下一步該怎麼做？
如果您準備好繼續開發，建議我們可以先從 **「階段 2：自動化部署與 GitHub Pages 上線」** 著手，把目前的成果發布到網路上；或是進入 **「階段 3：遊戲體驗優化 (加入成語釋義與音效/動畫)」** 來增加遊戲趣味性。
請告訴我您想先執行哪一項！
